[package]
edition = "2024"
name = "mathservice"
version = "0.1.0"
authors = ["Alexander Semmler"]

[lib]
crate-type = ["staticlib"]
path = "src/mathservice.rs"
test = false
doctest = false
bench = false

[dependencies]
# Local dependencies
ipc = { path = "../../library/ipc" }
runtime = { path = "../../library/runtime" }
terminal = { path = "../../library/terminal" }
concurrent = { path = "../../library/concurrent" }